// ═══════════════════════════════════════════════════════
// API CONFIG - КОНФИГУРАЦИЯ API
// ═══════════════════════════════════════════════════════
// Единое место для настройки URL backend
//
// Для Android эмулятора: 10.0.2.2
// Для iOS симулятора: localhost
// Для реального телефона: IP твоего Mac (192.168.x.x)
// ═══════════════════════════════════════════════════════

import { Platform } from 'react-native';

// Для эмулятора используем специальный IP
const getApiUrl = () => {
  if (Platform.OS === 'android') {
    // Android эмулятор
    return 'http://10.0.2.2:3000/api';
  } else if (Platform.OS === 'ios') {
    // iOS симулятор
    return 'http://localhost:3000/api';
  } else {
    // Web или другое
    return 'http://localhost:3000/api';
  }
};

export const API_URL = getApiUrl();

console.log('📡 API URL:', API_URL);