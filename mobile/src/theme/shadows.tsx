// ═══════════════════════════════════════════════════════
// SHADOWS - СИСТЕМА ТЕНЕЙ ДЛЯ IOS И ANDROID
// ═══════════════════════════════════════════════════════
// Файл: /Users/a00/mysterymeet/mobile/src/theme/shadows.ts
// 
// Что здесь:
// 1. shadows - универсальные тени (работают на обеих платформах)
// 2. platformShadows - платформенно-специфичные тени
// 
// Особенности:
// - iOS: использует shadow свойства (более качественные тени)
// - Android: использует elevation (нативные тени системы)
// ═══════════════════════════════════════════════════════

import { Platform } from 'react-native';

// ============================================
// SHADOWS - УНИВЕРСАЛЬНЫЕ ТЕНИ
// ============================================
// Эти тени работают на обеих платформах одновременно
// Используются когда нужна одинаковая тень везде

export const shadows = {
  // ============================================
  // NONE - БЕЗ ТЕНИ
  // ============================================
  none: {
    // Полное отсутствие тени - для плоских элементов
    shadowColor: 'transparent',
    shadowOffset: { width: 0, height: 0 },
    shadowOpacity: 0,
    shadowRadius: 0,
    elevation: 0,
  },

  // ============================================
  // SMALL - МАЛЕНЬКАЯ ТЕНЬ
  // ============================================
  small: {
    // Легкая тень для небольших подъемов
    // Используется для: карточек, кнопок в нормальном состоянии
    shadowColor: '#000',
    // Смещение вниз для эффекта "парения"
    shadowOffset: { width: 0, height: 2 },
    // Легкая прозрачность для мягкости
    shadowOpacity: 0.1,
    // Небольшой размытие
    shadowRadius: 4,
    // Android elevation (аналог тени)
    elevation: 2,
  },

  // ============================================
  // MEDIUM - СРЕДНЯЯ ТЕНЬ
  // ============================================
  medium: {
    // Заметная тень для среднего подъема
    // Используется для: модальных окон, всплывающих элементов
    shadowColor: '#000',
    // Большее смещение для большего "парения"
    shadowOffset: { width: 0, height: 4 },
    // Средняя прозрачность
    shadowOpacity: 0.15,
    // Большее размытие
    shadowRadius: 8,
    // Большее elevation на Android
    elevation: 4,
  },

  // ============================================
  // LARGE - БОЛЬШАЯ ТЕНЬ
  // ============================================
  large: {
    // Сильная тень для крупных подъемов
    // Используется для: плавающих кнопок, важных элементов
    shadowColor: '#000',
    // Максимальное смещение
    shadowOffset: { width: 0, height: 8 },
    // Высокая прозрачность
    shadowOpacity: 0.2,
    // Сильное размытие
    shadowRadius: 16,
    // Максимальное elevation на Android
    elevation: 8,
  },
};

// ============================================
// PLATFORM SHADOWS - ПЛАТФОРМЕННЫЕ ТЕНИ
// ============================================
// Эти тени оптимизированы под каждую платформу
// Используются когда нужны нативные тени системы

export const platformShadows = {
  small: Platform.select({
    // iOS тень - более качественная и плавная
    ios: {
      shadowColor: '#000',
      shadowOffset: { width: 0, height: 2 },
      shadowOpacity: 0.1,
      shadowRadius: 4,
    },
    // Android тень - использует системное elevation
    android: {
      elevation: 2,
    },
  }),

  medium: Platform.select({
    ios: {
      shadowColor: '#000',
      shadowOffset: { width: 0, height: 4 },
      shadowOpacity: 0.15,
      shadowRadius: 8,
    },
    android: {
      elevation: 4,
    },
  }),

  large: Platform.select({
    ios: {
      shadowColor: '#000',
      shadowOffset: { width: 0, height: 8 },
      shadowOpacity: 0.2,
      shadowRadius: 16,
    },
    android: {
      elevation: 8,
    },
  }),
};