// ═══════════════════════════════════════════════════════
// TYPOGRAPHY - ТИПОГРАФИКА И ТЕКСТОВЫЕ СТИЛИ
// ═══════════════════════════════════════════════════════
// Файл: /Users/a00/mysterymeet/mobile/src/theme/typography.ts
// 
// Что здесь:
// 1. Размеры шрифтов для разных типов текста
// 2. Веса шрифтов (жирность)
// 3. Высота линии (интерлиньяж)
// 4. Готовые текстовые стили для компонентов
// 
// Система размеров:
// - h1, h2, h3: заголовки разных уровней
// - body, caption, small: основной текст и подписи
// - button: текст для кнопок
// ═══════════════════════════════════════════════════════

export const Typography = {
  // ============================================
  // HEADINGS - ЗАГОЛОВКИ
  // ============================================
  h1: {
    // Самый крупный заголовок - главные заголовки страниц
    fontSize: 28,
    // Жирный шрифт для акцента
    fontWeight: '700' as const,
    // Высота линии для комфортного чтения
    lineHeight: 32,
  },
  
  h2: {
    // Заголовок среднего уровня - подзаголовки секций
    fontSize: 22, 
    // Полужирный для различия с основным текстом
    fontWeight: '600' as const,
    lineHeight: 28,
  },
  
  h3: {
    // Мелкий заголовок - заголовки карточек, списков
    fontSize: 18,
    fontWeight: '600' as const, 
    lineHeight: 24,
  },
  
  // ============================================
  // BODY - ОСНОВНОЙ ТЕКСТ
  // ============================================
  body: {
    // Стандартный размер для основного контента
    fontSize: 16,
    // Обычный вес для комфортного чтения
    fontWeight: '400' as const,
    // Оптимальная высота линии для читабельности
    lineHeight: 20,
  },
  
  bodyBold: {
    // Такой же размер как body, но жирный
    fontSize: 16,
    // Полужирный для выделения в тексте
    fontWeight: '600' as const,
    lineHeight: 20,
  },
  
  // ============================================
  // CAPTION - ПОДПИСИ И ВТОРОСТЕПЕННЫЙ ТЕКСТ
  // ============================================
  caption: {
    // Меньший размер для подписей, меток
    fontSize: 14,
    fontWeight: '400' as const, 
    lineHeight: 18,
  },
  
  captionBold: {
    // Жирные подписи для важной информации
    fontSize: 14,
    fontWeight: '600' as const,
    lineHeight: 18,
  },
  
  // ============================================
  // SMALL - МЕЛКИЙ ТЕКСТ
  // ============================================
  small: {
    // Самый мелкий текст - даты, номера, статичная информация
    fontSize: 12,
    fontWeight: '400' as const,
    lineHeight: 16,
  },
  
  // ============================================
  // BUTTON - ТЕКСТ ДЛЯ КНОПОК
  // ============================================
  button: {
    // Стандартный размер текста в кнопках
    fontSize: 16,
    // Полужирный для лучшей читаемости на кнопках
    fontWeight: '600' as const,
  },
  
  buttonSmall: {
    // Меньший размер для маленьких кнопок
    fontSize: 14,
    fontWeight: '600' as const, 
  }
};